# 📊 Telegram Currency Tracker Bot
Бот для отслеживания изменения курса валют за выбранный период (неделя, месяц, год).
Работает через API **Fixer.io** и библиотеку **pyTelegramBotAPI (telebot)**.

## 🚀 Возможности бота
- Выбор целевой валюты (USD, KZT, RUB, GBP, JPY и другие).
- Анализ изменения курса EUR → выбранная валюта.
- Периоды:
  - 7 дней
  - 30 дней
  - 365 дней
- Подробный вывод:
  - Текущий курс
  - Курс в прошлом
  - Разница в значениях
  - Процент изменения
- Удобная навигация через Inline-кнопки.

## 📦 Установка
### 1. Клонируйте репозиторий
```bash
git clone https://github.com/username/repository.git
cd repository
```

### 2. Установите зависимости
```bash
pip install -r requirements.txt
```

Если файла `requirements.txt` нет, зависимости такие:
```bash
pip install pyTelegramBotAPI requests
```

## 🔑 Конфигурация
Перед запуском необходимо создать и добавить свои ключи:

### В файле `bot.py` замените:
```python
TOKEN = "ВАШ_TG_ТОКЕН"
API_KEY = "ВАШ_API_КЛЮЧ_FIXER_IO"
```

## ▶ Запуск бота
```bash
python bot.py
```

## 🧩 Структура проекта
```
project/
│── bot.py
│── README.md
│── requirements.txt 
```

## 📝 Как работает бот
1. Пользователь вводит `/start`.
2. Бот показывает список валют.
3. Пользователь выбирает валюту.
4. Бот предлагает выбрать период.
5. Бот получает курсы через Fixer API:
   - текущую дату
   - дату X дней назад
6. Выводит детальную статистику об изменении курса.

## ❗ Возможные ошибки
- Некорректный API-ключ Fixer.io
- Превышение лимита запросов
- Пользователь выбрал валюту, отсутствующую в API
- Ошибки сети (таймаут, недоступное API)

## 📬 Обратная связь
Если хочешь — могу добавить:
- деплой на PythonAnywhere/Render
- автозапуск через systemd
- хранение токенов в `.env`
- версию бота с графиками
